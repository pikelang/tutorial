<metadata>
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Programs are Classes and Vice Versa">
</metadata>
<manual>

<p>It may surprise you to know that you have already seen several
class definitions in this tutorial. If you have written a program in
Pike, you have also written a class. This is because in Pike, programs
and classes are the same, and the terms <b>program</b> and
<b>class</b> are sometimes used as synonyms.</p>

<p>A Pike program in a file is a class definition. The methods that
you have defined in the file are the methods of the class, and the
global variables (that is, the variables defined outside the methods)
are the member variables. If you like, you can imagine that the file
has an implicit "<pi>class { }</pi>" around the code. But it would be
bothersome if we had to put every little class in its own file, so we
also have the "<pi>class { }</pi>" notation.</p>

<p>To create the equivalent of the class <tt>animal</tt>, which we
defined above, we would need a file with the following contents. The
file name can be "animal.pike", but any name will work.</p>

<pi>
string name;

float weight;

void create(string n, float w)
{
  name = n;
  weight = w;
}

void eat(string food)
{
  write(name + " eats some " + food + ".\n");
  weight += 0.5;
}
</pi>

<p>We can't use a string as a type name, so the file name won't work
as a data type:</p>

<pi>"animal.pike" my_dog; // Doesn't work at all.</pi>

<p>Instead, we must let Pike read and compile the file, making a
<b>program</b> (or <b>class</b>, if you prefer that term, but the data
type is <tt>program</tt>) of it, and then we put that program in a
<b>constant</b>:</p>

<pi>constant animal = (program)"animal.pike";</pi>

<p>Now you can use <tt>animal</tt> as the class name, just as
before:</p>

<pi>animal piglet = animal("Piglet", 6.3);</pi>

</manual>
