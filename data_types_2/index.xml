<p>These are the <b>basic types</b> in Pike:</p>

<list type="ul">
<item><p>integer (written <tt>int</tt> in Pike)</p></item>
<item><p>floating-point number (written <tt>float</tt>)</p></item>
<item><p>string (written <tt>string</tt>)</p></item>
</list>

<p>They are basic in the sense that data items of these data types
can't contain other data items. When a data item of a basic type
is stored in a variable, it is the data item itself that is stored,
and not just a reference to it, as explained earlier.</p>

<h1>The Data Type <tt>int</tt></h1>

<p>Integer values can be written in several ways:</p>

<list type="ul">
<item>
<p>In decimal (that is, base 10) format, i. e. the normal way to write
numbers, such as <tt>78</tt>.</p>
</item>

<item>
<p>In octal (that is, base 8) format, with a leading <tt>0</tt>, such
as <tt>0116</tt> (which is equal to <tt>78</tt> in decimal
notation).</p>
</item>

<item>
<p>In hexadecimal (that is, base 16) format, with a leading
<tt>0x</tt>, such as <tt>0x4e</tt> (which is also equal to <tt>78</tt>
in decimal notation).</p>
</item>

<item>
<p>In binary (ones and zeroes) format, with aleading <tt>0b</tt>, such
as <tt>0b1001110</tt> (which again is equal to <tt>78</tt> in decimal
notation).</p>
</item>

<item>
<p>As a character literal within single quotes, which will give the
internal character code for that character. For example, <tt>'N'</tt>
will give the character code for the character <b>N</b>, which is
usually equal to <tt>78</tt>.</p>
</item>
</list>

<p>You can use normal arithmetic operations, such as addition and
multiplication, with integers, but you can also consider integers as
sequences of bits and, apply bitwise operations on them.</p>

<p>For example, the integer value <b>247</b> is stored internally as
the sequence of bits <b>1111011</b>, and you could left-shift it three
positions with the expression <tt>247 &lt;&lt; 3</tt>, giving the bit
pattern <b>1111011000</b>, which is equal to <b>1976</b>.</p>

<p>Integers in Pike can be very large. For small integers (usually
less than 2 billion or so), Pike will use the computer's own,
hardware-supported way of representing integers. For larger integers,
Pike will use "bignums". These are slower, but can be arbitrarily
large. Just like the smaller integers, bignums are represented
exactly, without any rounding errors.</p>

<p>As a programmer, you will usually not need to worry about the
difference between bignums and smaller integers. There may however be
some operations, for example certain methods in certain modules, that
cannot handle bignums but work with smaller integers.</p>

<p>Some operations that you can apply to integers:</p>

<list type="ul">
<item name="Check if it is an integer">
<pi>intp(<i>something</i>)</pi>

<p>The function <tt>intp</tt> returns <b>1</b> if the value
<i>something</i> is an integer, otherwise <b>0</b>. Example:</p>

<pi>
if(intp(u))
  write("The integer is " + u + ".\n");
else
  write("It is not an integer.\n");
</pi>
</item>

<item name="Get a random number">
<p><pi>random(<i>limit</i>)</pi> returns a random value which is
greater or equal to <b>0</b> and less than the integer
<i>limit</i>.</p>
</item>

<item name="Reverse the bits">
<p><pi>reverse(<i>integer-value</i>)</pi> returns an integer with the
bits in the <i>integer-value</i> in reverse order.</p>
</item>
</list>

<h1>The Data Type <tt>float</tt></h1>

<p>Real numbers, which are numbers that can have a fractional part,
such as <b>18.34</b> and <b>-1000.03</b>, are represented in the
computer as <b>floating-point numbers</b>. Floating-point numbers can
be used to represent very large numbers, but with limited precision.
The number of significant digits is the same, no matter the magnitude
of the value.</p>

<p>You can write floating-point values in two ways, as usual with
decimals, or in exponential form with an <tt>e</tt>:</p>

<pi>
3.1415926535
-123.0001
12.0
1.0e6 // 1.0 times 10 to the power of 6, i. e. one million
2.0e-6 // 2.0 times 10 to the power of -6, i. e. two one-millionths
-1.0e-2 // Minus one hundredth
</pi>

<p>Some operations that you can apply to floating-point numbers:</p>

<list type="ul">
<item name="Check if it is a floating-point number">
<pi>floatp(<i>something</i>)</pi>

<p>The function <tt>floatp</tt> returns <b>1</b> if the value
<i>something</i> is a floating-point number, otherwise <b>0</b>.</p>
</item>

<item name="Round downwards">
<p><pi>floor(<i>float-value</i>)</pi> returns the largest integer that
is less than or equal to <i>float-value</i>, but as a floating-point
number. For example, <tt>floor(7.3)</tt> gives <b>7.0</b> (and not the
integer <b>7</b>). If you do want the integer value, you can use an
explicit type conversion: <tt>(int)7.3</tt> gives you the integer
<b>7</b>.</p>
</item>

<item name="Round upwards">
<p><pi>ceil(<i>float-value</i>)</pi> returns the smallest integer that
is greater than or equal to <i>float-value</i>, as a floating-point
number. For example, <tt>ceil(7.3)</tt> gives <b>8.0</b> (and not the
integer <b>8</b>).</p>
</item>
</list>

<h1>The Data Type <tt>string</tt></h1>

<p>Strings are sequences of characters, and are written within double
quotes (<tt>"</tt>) in Pike:</p>

<pi>
"scorch"
"Hello world!"
"Woe to you, oh Earth and Sea"
""
</pi>

<p>The last one is the empty string. Special characters, such as the
double quote character, need to be preceded by a backslash character
(<tt>\</tt>):</p>

<list type="ul">
<item><p><tt>\"</tt> to get a double quote (<tt>"</tt>) in the string</p></item>
<item><p><tt>\\</tt> to get a backslash character (<tt>\</tt>)</p></item>
<item><p><tt>\n</tt> to get a newline character</p></item>
<item><p><tt>\t</tt> to get a tab character</p></item>
<item><p><tt>\r</tt> to get a carriage return</p></item>
<item>
<p><tt>\0</tt> to get a NUL character, i. e. the character with
character code <b>0</b></p>
</item>
</list>

<p>Here are some examples of strings:</p>

<pi>
"One line\nAnother line\nA third line"
"Strings are written within \" characters."
</pi>

<p>You can use character codes instead of the characters themselves.
If you write \d followed by a decimal (that is, normal base 10) number,
it will be replaced by the character with that character code.
The same goes for \x followed by a hexadecimal (base 16) number,
and a single \ followed by an octal (that is, base 8) number.
The <tt>\0</tt> is actually an example of this.
These four strings are identical:</p>

<pi>
"Hello world"
"Hello \d119orld"
"Hello \x77orld"
"Hello \167orld"
</pi>

<p>Strings in Pike are what we call "shared", which means that if two
or more strings contain exactly the same characters, there will still
be only one copy of it.</p>

<p>Some operations that you can apply to strings:</p>

<list type="ul">
<item name="Check if it is a string">
<pi>stringp(<i>something</i>)</pi>

<p>The function <tt>stringp</tt> returns <b>1</b> if the value
<i>something</i> is a string, otherwise <b>0</b>.</p>
</item>

<item name="Concatenation">
<p>Strings can be concatenated with the <tt>+</tt> operator:</p>

<pi>
string s1 = "Hello";
string s2 = " ";
string s3 = "world!";
write(s1 + s2 + s3 + "\n");
</pi>
</item>

<item name="Concatenation of string literals">
<p><b>String literals</b>, who are strings within double quotes that
are written in a program, can be concatenated by just putting them
after each other:</p>

<pi>write("Hello"  " "  "world!" + "\n");</pi>
</item>
</list>

<p>Pike has many powerful built-in operations for working with
strings. Read more about those in the chapter about string handling,
below.</p>
