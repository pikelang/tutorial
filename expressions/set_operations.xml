<metadata>
 <datum name=__commit_id valuen=19135>
 <datum name=__created valuen=0>
 <datum name=__ext_created valuen=1411269018>
 <datum name=__uuid value="eec6b4e6-b311-4fcc-a495-19284fffa781">
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Operations on Sets">
</metadata>
<manual>

<p>Some of the operators that are used for bitwise operations
(<tt>|</tt>, <tt>&amp;</tt> and <tt>^</tt>) can also be used for
operations on sets, such as union and intersection. They can be
applied to multisets, but also to arrays and mappings.</p>

<xtable>

<row>
<h>Operation</h>
<h>Syntax</h>
<h>Result</h>
</row>

<row>
<c><p>Intersection (and)</p></c>
<c><p>a &amp; b</p></c>
<c><p>All elements present in both a and b.</p></c>
</row>

<row>
<c><p>Union (or)</p></c>
<c><p>a | b</p></c>
<c><p>All elements present in at least one of a and b.</p></c>
</row>

<row>
<c><p>Symmetric difference (exclusive or)</p></c>
<c><p>a ^ b</p></c>
<c><p>All elements present in a or b, but not present in both.</p></c>
</row>

</xtable>

<p>Two calculate the difference between two multisets, arrays or
mappings, you use the <tt>-</tt> operator:</p>

<xtable>

<row>
<h>Operation</h>
<h>Syntax</h>
<h>Result</h>
</row>

<row>
<c><p>difference</p></c>
<c><p>a - b</p></c>
<c><p>All elements present in a, but not in b.</p></c>
</row>

</xtable>

<p>When using mappings in set operations, we only consider the
indices. The values are copied along with the indices. If an index is
present in both mappings in a union or intersection, the one from the
right-side mapping will be used.</p>

<p>Some examples:</p>

<p><pi>
mapping m1 = ([ 1:"one", 2:"two" ]),
        m2 = ([ 2:"TWO", 3:"THREE" ]);
</pi></p>

<xtable>

<row>
<h>Expression</h>
<h>Result</h>
</row>

<row>
<c><p><pi>m1 &amp; m2</pi></p></c>
<c><p><pi>([ 2:"TWO" ])</pi></p></c>
</row>

<row>
<c><p><pi>m1 | m2</pi></p></c>
<c><p><pi>([ 1:"one", 2:"TWO", 3:"THREE" ])</pi></p></c>
</row>

<row>
<c><p><pi>m1 + m2</pi></p></c>
<c><p><pi>([ 1:"one", 2:"TWO", 3:"THREE" ])</pi></p></c>
</row>

<row>
<c><p><pi>m1 ^ m2</pi></p></c>
<c><p><pi>([ 1:"one", 3:"THREE" ])</pi></p></c>
</row>

<row>
<c><p><pi>m1 - m2</pi></p></c>
<c><p><pi>([ 1:"one" ])</pi></p></c>
</row>

</xtable>


<p>You can also use the operator <tt>+</tt> on multisets, arrays and
mappings. For multisets and mappings, it calculates the union, i e the
same as the <tt>|</tt> operator. For arrays, it gives a different
result: it just concatenates the arrays, instead of calculating the
union:</p>

<xtable>

<row>
<h>Expression</h>
<h>Result</h>
</row>

<row>
<c><p><pi>({ 1, 2, 4 }) | ({ 3, 4 })</pi></p></c>
<c><p><pi>({ 1, 2, 3, 4 })</pi></p></c>
</row>

<row>
<c><p><pi>({ 1, 2, 4 }) + ({ 3, 4 })</pi></p></c>
<c><p><pi>({ 1, 2, 4, 3, 4 })</pi></p></c>
</row>

</xtable>

</manual>
