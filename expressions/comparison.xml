<manual>

<p>Pike lets you compare things to each other.</p>

<xtable>

<row>
<h>Operation</h>
<h>Syntax</h>
<h>Result</h>
</row>

<row>
<c><p>Same</p></c>
<c><p>a == b</p></c>
<c><p>1 if a is the same value as b, 0 otherwise</p></c>
</row>

<row>
<c><p>Not same</p></c>
<c><p>a != b</p></c>
<c><p>0 if a is the same value as b, 1 otherwise</p></c>
</row>

<row>
<c><p>Greater than</p></c>
<c><p>a &gt; b</p></c>
<c><p>1 if a is greater than b, 0 otherwise</p></c>
</row>

<row>
<c><p>Greater than or equal to</p></c>
<c><p>a &gt;= b</p></c>
<c><p>1 if a is greater to or equal to b, 0 otherwise</p></c>
</row>

<row>
<c><p>Lesser than</p></c>
<c><p>a &lt; b</p></c>
<c><p>1 if a is less than b, 0 otherwise</p></c>
</row>

<row>
<c><p>Lesser than or equal to</p></c>
<c><p>a &lt;= b</p></c>
<c><p>1 if a is less than or equal to b, 0 otherwise</p></c>
</row>

</xtable>

<p>As you may remember, Pike interprets the value zero (<b>0</b>) as
<b>false</b>, and everything else is interpreted as <b>true</b>. The
comparison operators give a truth value as result, and to simplify
things they always return <b>0</b> for <b>false</b> and <b>1</b> for
<b>true</b>.</p>

<p>The operators <tt>==</tt> and <tt>!=</tt> can be used on any type.
Note that these two operators check if two things are the <i>same</i>,
not just if they are <i>equal</i>. For two things to be the same,
first of all they have to have the same type. For example, <b>1</b>
and <b>1.0</b> may be equal, but they are not the same.</p>

<p>All values of the basic types (integers, floating-point values, and
strings) that are of the same type and that are equal, are also the
same. The expressions <tt>7 == 7</tt> and <tt>"hi" == "hi"</tt> will
always be <b>true</b>. With the other types, the so-called reference
types, it is more difficult: Two arrays can have the same length and
contain the same elements, but still be two separate arrays. For a
comparison like <tt>a1 == a2</tt> to be true, where <tt>a1</tt> and
<tt>a2</tt> are array variables, they must refer to the same array,
not just two similar arrays. (See also the section "The difference
between basic types and reference types" in the chapter about data
types.)</p>

<p>To check if two things are similar, even if they are not
necessarily the same, you can use the function <tt>equal</tt>. This
comparison will be true:</p>

<pi>equal( ({ 7, 1, ({ 7 }) }), ({ 7, 1, ({ 7 }) }) )</pi>

<p>but this one will be false:</p>

<pi>({ 7, 1, ({ 7 }) }) == ({ 7, 1, ({ 7 }) })</pi>

<p>The relational operators
<tt>&gt;</tt>,
<tt>&gt;=</tt>,
<tt>&lt;</tt>, and
<tt>&lt;=</tt>
can only be used with integers, floating-point values, and strings. If
you compare an integer with a floating-point value, the integer will
be converted to a floating-point value before the comparison.</p>

<p>When comparing strings, lexical or "alphabetical" order is used.
Different countries sort their alphabets in different order, and if
your operating system supports this, Pike will too. For example, in
Unix you can set the environment variables <tt>LC_CTYPE</tt> and
<tt>LC_LANG</tt>, and Pike will use their values to determine the
right sorting order.</p>

</manual>
