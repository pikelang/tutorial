<metadata>
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Type Conversions">
</metadata>
<manual>

<p>Values of one type can be converted to a value of another type. For
example, the integer <tt>4711</tt> can be converted to the string
<tt>"4711"</tt>. (These two may look similar, but they are actually
entirely different things. For example, <tt>4711 + 3</tt> will give
you <tt>4714</tt>, <tt>"4711" + "3"</tt> will give you
<tt>"47113"</tt>.)</p>

<p>Some conversions are done automatically. For example, in the
expression <tt>"hello" + 17</tt> the integer <tt>17</tt> will be
automatically converted to the string <tt>"17"</tt>, before the two
strings are concatenated, giving the result <tt>"hello17"</tt>. But
some conversions need to be performed with a special operation, called
an <b>explicit type conversion</b> or <b>cast</b>. There is a special
<b>cast operator</b>, which is written as a data type between
parentheses:</p>

<pi>
(float)14	// Gives the float 14.0
(int)6.9	// Gives the int 6
(string)6.7	// Gives the string "6.7"
(float)"6.7"	// Gives the float 6.7
</pi>

<p>Here is a list of some useful casts:</p>

<xtable>

<row>
<h>Casting from</h>
<h>to</h>
<h>gives the result</h>
</row>

<row>
<c><p>int</p></c>
<c><p>float</p></c>
<c><p>The floating-point number with the same value as the integer</p></c>
</row>

<row>
<c><p>float</p></c>
<c><p>int</p></c>
<c><p>An integer, with the fractional part ("the decimals") just
skipped</p></c>
</row>

<row>
<c><p>int</p></c>
<c><p>string</p></c>
<c><p>Converts the integer to a string with a number in normal base-10
notation</p></c>
</row>

<row>
<c><p>float</p></c>
<c><p>string</p></c>
<c><p>Converts the floating-point number to a string</p></c>
</row>

<row>
<c><p>string</p></c>
<c><p>int</p></c>
<c><p>Converts the first number in the string to an integer. The number
should be an integer written in normal base-10 notation.</p></c>
</row>

<row>
<c><p>string</p></c>
<c><p>float</p></c>
<c><p>Converts the first number, with or without decimals, in the string
to a floating-point number</p></c>
</row>

<row>
<c><p>string</p></c>
<c><p>program</p></c>
<c><p>If the string contains a filename, compile the file and return the
program. The results are cached.</p></c>
</row>

<row>
<c><p>string</p></c>
<c><p>object</p></c>
<c><p>First casts the string to a program as above, and then clones the
result. The results are cached.</p></c>
</row>

<row>
<c><p>array</p></c>
<c><p>array(<i>type</i>)</p></c>
<c><p>This recursively casts all values in the array to <i>type</i>.</p></c>
</row>

<row>
<c><p>multiset</p></c>
<c><p>array</p></c>
<c><p>The same as doing <tt>indices(<i>multiset</i>)</tt>.</p></c>
</row>

</xtable>

<p>Sometimes you know more than what Pike does about data types, and
another use of the cast operator is to tell Pike what you know. For
example, lets say that you have a variable with the type
<tt>mixed</tt>, which means that it can contain any type of data. Lets
also say that you happen to know that that variable contains an
integer. If you want to use this value where an integer is required,
Pike might complain about it:</p>

<pi>
mixed m;
int i;
m = 18;
i = m;	// This might make Pike suspicious
</pi>

<p>You can use a cast to tell Pike that <tt>m</tt> is an integer and
nothing else:</p>

<pi>i = (int)m; // Don't worry. I promise it's an int!</pi>

</manual>
