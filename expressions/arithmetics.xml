<metadata>
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Arithmetical Operations">
</metadata>
<manual>

<p>Pike can do all the usual arithmetic operations: addition (which is
expressed with the operator <tt>+</tt>), subtraction (<tt>-</tt>),
division (<tt>/</tt>), multiplication (<tt>*</tt>), and also modulo
(<tt>%</tt>). The modulo operation, sometimes called "remainder" or
"rest", gives the remainder from a division. For example, if you
divide 7 by 3, 3 goes in 7 two times. But there is a remainder of 1,
and that is the result of the modulo operation.</p>

<p>Here is a table of the arithmetic operations:</p>

<xtable>
<row><h>Operation</h>  <h>Syntax</h> <h>Result</h></row>
<row><c><p>Addition</p></c>   <c><p>a + b</p></c>  <c><p>the sum of a and b</p></c></row>
<row><c><p>Subtraction</p></c><c><p>a - b</p></c>  <c><p>b subtracted from a</p></c></row>
<row><c><p>Negation</p></c>   <c><p>- a</p></c>    <c><p>minus a</p></c></row>
<row><c><p>Multiplication</p></c><c><p>a * b</p></c> <c><p>a multiplied by b</p></c></row>
<row><c><p>Division</p></c>   <c><p>a / b</p></c>  <c><p>a divided by b</p></c></row>
<row><c><p>Modulo</p></c>     <c><p>a % b</p></c>
<c><p>the remainder of a division between a and b</p></c></row>
</xtable>

<p>Remember that pike makes a difference between numerical values that
are integers (the type <tt>int</tt>), and numerical values that are
real or "floating-point" numbers (the type <tt>float</tt>). This has
some importance for how expressions with arithmetic operations are
calculated by Pike. If at least one of the operands is a
<tt>float</tt>, we use the floating-point versions of the operation.
In if both operands are integers, we use a special integer-only
version of the operation. For most of the operations, the only
difference is that the type of the result will be different: <tt>2 +
2</tt> will give the integer value <b>4</b>, while <tt>2 + 2.0</tt>
will give the floating-point value <b>4.0</b>. But with division is
more important.</p>

<p>With <b>floating-point division</b>, such as in <tt>9.0 / 4.0</tt>,
the result is a floating-point value, in this case <b>2.25</b>. But
with <b>integer division</b>, such as in <tt>9 / 4</tt>, the result is
only the integer part, in this case <b>2</b>.</p>

<p>The fact that integer division only gives the integer part can be
treacherous: If 73 out of 92 people payed their income tax on time,
don't try to calculate the percentage with the expression <tt>73 / 92
* 100</tt>. That would give the result <b>0</b>.</p>

<p>It is very common in programs to increment or decrement a variable
with one, such as in the statements</p>

<pi>
i = i + 1;
p = p - 1;
</pi>

<p>To simplify such programs, Pike has these extra operators, which
you can use if you want to:</p>

<xtable>

<row>
<h>Operation</h>
<h>Syntax</h>
<h>Result</h>
</row>

<row>
<c><p>Increment</p></c>
<c><p>++ a</p></c>
<c><p>increments a and returns the new value for a</p></c>
</row>

<row>
<c><p>Decrement</p></c>
<c><p>-- a</p></c>
<c><p>decrements a and returns the new value for a</p></c>
</row>

<row>
<c><p>Post increment</p></c>
<c><p>a ++</p></c>
<c><p>increments a and returns the old value for a</p></c>
</row>

<row>
<c><p>Post decrement</p></c>
<c><p>a --</p></c>
<c><p>decrements a and returns the old value for a</p></c>
</row>

</xtable>

<p>The two versions of increment, <tt>++i</tt> and <tt>i++</tt>, both
increment the value in the variable <tt>i</tt> with 1. The difference
is if we want to use them as parts of a larger expression. In that
case, <tt>++i</tt> gives the new, incremented, contents of <tt>i</tt>
as value, while <tt>i++</tt> gives the old contents of <tt>i</tt> as
value. The same difference applies to <tt>--i</tt> and
<tt>i--</tt>.</p>

</manual>
