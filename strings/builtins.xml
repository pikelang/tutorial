<manual>

<p>The functions <tt>sprintf</tt> and <tt>sscanf</tt> will be
presented in their own sections, below.</p>

<list type="ul">
<item name="Check if it is a string">
<pi>stringp(<i>something</i>)</pi>

<p>The function <tt>stringp</tt> returns <b>1</b> if the value
<i>something</i> is a string, otherwise <b>0</b>.</p>
</item>

<item name="Finding the size">
<p>
<pi>sizeof(<i>string</i>)</pi> or
<pi>strlen(<i>string</i>)</pi>
</p>

<p>Returns the length (that is, the number of characters) in the
string <i>string</i>.</p>

<p>
<pi>sizeof("hi ho")</pi> gives the result <pi>5</pi>.
<pi>sizeof("")</pi> gives the result <pi>0</pi>.
</p>

<p>The function <tt>strlen</tt> is a synonym for <pi>sizeof</pi>.</p>
</item>

<item name="Reversing a string">
<p><pi>reverse(<i>string</i>)</pi> returns a new string with the
characters in reverse order.</p>

<p>
<pi>reverse("foo")</pi> gives the result <pi>"oof"</pi>.
</p>
</item>

<item name="Replacing parts in a string">
<p><pi>replace(<i>string</i>, <i>old</i>, <i>new</i>)</pi> returns a
new string where all occurrences of the string <i>old</i> have been
replaced with the string <i>new</i>:</p>

<p>
<pi>replace("fooFOOfoo", "foo", "fum")</pi> gives the result
<pi>"fumFOOfum"</pi>.
</p>
</item>

<item name="Converting to lower case">
<p><pi>lower_case(<i>string</i>)</pi> returns a new string where all
upper-case characters in the string <i>string</i> have been turned to
lower case:</p>

<p>
<pi>lower_case("A Foo IS!")</pi> gives the result
<pi>"a foo is!"</pi>.
</p>
</item>

<item name="Converting to upper case">
<p><pi>upper_case(<i>string</i>)</pi> returns a new string where all
lower-case characters in the string <i>string</i> have been turned to
upper case:</p>

<p>
<pi>upper_case("A Foo IS!")</pi> gives the result
<pi>"A FOO IS!"</pi>.
</p>
</item>

<item name="Capitalizing">
<pi>String.capitalize(<i>string</i>)</pi>

<p>If the first character in the string <i>string</i> is a lower-case
character, it is converted to upper case:</p>

<p>
<pi>String.capitalize("xyz-Foo")</pi>
gives the result
<pi>"Xyz-Foo"</pi>.
</p>
</item>

<item name="Finding a substring in a string">
<p><pi>search(<i>haystack</i>, <i>needle</i>)</pi> returns the index
of the start of the first occurrence of the string <i>needle</i> in
the string <i>haystack</i>:</p>

<p>
<pi>search("sortohum", "orto")</pi> gives the result <pi>1</pi>.
</p>
</item>
</list>

</manual>
