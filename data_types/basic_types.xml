<p>We have already seen integers (<tt>int</tt>) and strings
(<tt>string</tt>) being used in several examples, but the third basic
type, <tt>float</tt>, is new. A <tt>float</tt>, also called a <b>real
number</b> or a <b>floating-point number</b>, is different from an
integer in that it can have a fraction part:</p>

<pi>
6.783   // This is a floating-point number
17      // This is an integer
17.0    // This is a floating-point number
</pi>

<p>Note that Pike differentiates between integer and floating-point
numbers that happen to be equal to an integer. If you write
<tt>17</tt> in a Pike program you get an integer, and if you write
<tt>17.0</tt> you get a floating-point number. Inside the computer
they look completely different.</p>

<p>You can define variables like this:</p>

<pi>
int number_of_monkeys;  // An integer variable
float z = -16.2;        // A floating-point variable
string file_name;       // A string variable
mixed x;                // A variable for anything
</pi>

<p>The data type <tt>mixed</tt> means "any type of value".</p>
