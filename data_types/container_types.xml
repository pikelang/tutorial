<metadata>
 <datum name=__commit_id valuen=19157>
 <datum name=__created valuen=0>
 <datum name=__ext_created valuen=1411269025>
 <datum name=__uuid value="b354a063-bc7f-4995-9d0c-ccd93f66cd2c">
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Container Types">
</metadata>
<manual>

<p>Some of the data types are <b>containers</b>. A data item of one of
these container types can contain other data items.</p>

<p>The simplest of the container types is the <b>array</b>. If a
variable is a box where you can put a data item, an array is a whole
sequence of such boxes. The boxes are numbered, starting with 0, so in
an array with 10 places the first one has number 0, and the tenth and
last one has number 9. The position numbers are called <b>indices</b>.
(One <b>index</b>, many indices.)</p>

<p>You can give an array in your Pike program by listing its elements
inside parenthesis-curly-bracket quotes:</p>

<pi>
({ "geegle", "boogle", "fnordle blordle" })
({ 12, 17, -3, 8 })
({  })
({ 12, 17, 17, "foo", ({ "gleep", "gleep" }) })
</pi>

<p>As you can see, an array can be empty (containing no elements), and
it can also contain other arrays. An array can contain elements of
different types, but it is more common with arrays that only contain
one type of element.</p>

<p>You can define array variables like this:</p>

<pi>
array a;		// Array of anything
array(string) b;	// Array of strings
array(array(string)) c;	// Array of arrays of strings
array(mixed) d;		// Array of anything
</pi>

<p>Then you can give values to these variables:</p>

<pi>
a = ({ 17, "hello", 3.6 });
b = ({ "foo", "bar", "fum" });
c = ({ ({ "John", "Sherlock" }), ({ "Holmes" }) });
d = c;
</pi>

<p>You can access the elements in an array, either to just get the
value or to replace it. This is usually called <b>indexing</b> the
array. Indexing is done by writing the position number, or index,
within square brackets after the array:</p>

<pi>
write(a[0]);	// Writes 17
b[1] = "bloo";	// Replaces "bar" with "bloo"
c[1][0] = b[2];	// Replaces "Holmes" with "fum"
</pi>

<p>There are many useful operations that can be done with arrays. For
example, you can add two arrays together:</p>

<pi>({ 7, 1 }) + ({ 3, 1, 6 })</pi>

<p>The result of that expression is a new array, with the elements
from the two arrays concatenated:</p>

<pi>({ 7, 1, 3, 1, 6 })</pi>

<p>Except for arrays there are two other container types in Pike,
<tt>mapping</tt> and <tt>multiset</tt>. A set is something where a
value is either a member or not, and a multiset is a set which can
contain several copies of the same value. Mappings are sometimes
called dictionaries, and lets you translate from one value (such as
<tt>"beer"</tt>) to another value (<tt>"cerveza"</tt>). Academics
might know the datatype by the name "associative array", Perl
programmers call them "hashes". Mappings and multisets are explained
in a later chapter.</p>

</manual>
