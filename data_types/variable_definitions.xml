<metadata>
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Variable Definitions">
</metadata>
<manual>

<h1>Zero is special</h1>

<p>The value zero (<b>0</b>) is a special case. It is not just an
integer value, but can also be used to mean "no value" for all the
types, and not just integers. If you create a variable and don't put a
value in it, it starts with the value <b>0</b>.</p>

<p>Some effects of this may be surprising to C programmers:</p>

<pi>
float f;  // Now, f contains the <b>integer</b> value 0.
f = f + 1;// Now, f contains the <b>integer</b> value 1.
f = 0;    // Now, f contains the <b>integer</b> value 0 again!
f = 0.0;  // Now, f contains the <b>real</b> value 0.0.
</pi>

<p>It's important to note that the integer value 0 and the real value
0.0 are not equal to one another in Pike. It is also worth noting that
uninitialized strings, as all other uninitialized variables, start at
0. If you don't take this into account, and write code that
successively builds some form of answer, reply or similar:</p>

<pi>
string accumulate_answer()
{
  string result;
  // Probably some code here
  result += "Hi!\n";
  // More code goes here
  return result;
}
</pi>

<p>you will get a string <pi>"0Hi!\n"</pi>, since Pike will try to
make the best of the situation, adding your string to the integer 0.
Had you instead initialized the variable to <pi>""</pi>, as in
<pi>string result = "";</pi>, you'd get a better result.</p>

<h1>How to specify data types</h1>

<p>In general, you just use the name of the data type:</p>

<pi>
int number_of_wolves;
Protocols.HTTP.Query this_web_page;
float simple_plus(float x, float y) { return x + y; }
</pi>

<p>You can also use the data type <tt>mixed</tt>, meaning any type of
value:</p>

<pi>
mixed x;
array(mixed) build_array(mixed x) { return ({ x }); }
</pi>

<p>The variable <tt>x</tt> can contain any type of value. The method
<tt>build_array</tt> takes an argument of any type, and returns it
inside an array.</p>

<p>Pike also lets you use "or" notation for datatypes, saying that a
value is one of several possible datatypes:</p>

<pi>
int|string w;

array(int|string|float)
make_array(int|string|float x)
{
  return ({ x });
}
</pi>

<p>The variable <tt>w</tt> can contain either an integer or a string.
The method <tt>make_array</tt> takes an integer, a string or a
floating-point number as argument, and returns it inside an array.</p>

<p>If you know that a variable will contain strings or integers, it is
usually better to use <tt>string|int</tt> than <tt>mixed</tt>. It is
slightly longer to write, but it allows Pike to do type checking when
the program is compiled and executed, thus helping you to ensure that
your program works as it should.</p>

<p>Note that if one of the types in an "or" expression of this kind is
a type from inside a module, so that the type contains a dot, the
simple syntax doesn't work:</p>

<pi>Protocols.HTTP.Query|string q; // Error!</pi>

<p>You have to use the alternative syntax
<tt>object(<i>class-name</i>)</tt> instead of just
<tt><i>class-name</i></tt> for the class:</p>

<pi>object(Protocols.HTTP.Query)|string q;</pi>

</manual>
