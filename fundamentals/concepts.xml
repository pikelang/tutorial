<metadata>
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Terminology / Glossary">
</metadata>
<manual>

<h1>Program</h1>

<p>A program consists of a set of instructions that tells the computer
how to do something, plus some data that the program can work with.
Programs can be organized in different ways, and are often divided
into several smaller parts.</p>

<h1>Data</h1>

<p>The things that your program works with, such as integers (for
example <b>5</b> and <b>-3</b>) and character strings (such as "hello"
and "Hi there, John!") are data.</p>

<h1>Value</h1>

<p>One piece of data (for example the integer <b>5</b>) is sometimes
called a <b>value</b>, and sometimes a <b>data item</b> or <b>data
object</b>. (But the word <b>object</b> also has a more specialized
meaning in connection with object-oriented programming. More about
that later.)</p>

<h1>Variable</h1>

<p>A variable is a sort of box that can be used to store a value. The
variable usually has a name and also a type. The type determines which
values you can put in the variable.</p>

<h1>Constant</h1>

<p>A constant is in a way the opposite of a <b>variable</b>, but also
similar. A constant is like a variable, in the sense that it is a sort
of box that can be used to store a value, and that it has a name. But
once you have defined it, it can not be changed. Constants are often
used to give names to certain values in order to make a program easier
to understand: <tt>minimum_income_tax</tt> somewhere in a program is
easier to understand than just <tt>20000</tt>. Sometimes the term
constant is used to include <b>literals</b> too.</p>

<h1>Literal</h1>

<p>A literal is a value of some kind, written in the program.
<tt>"Hello"</tt> is a string literal, <tt>-678</tt> is an integer
literal, and <tt>({ 7, 8, 9 })</tt> is an array literal. Sometimes the
term <b>constant</b> is used to include literals too.</p>

<h1>Identifier</h1>

<p>A sequence of characters that can be used in a program as a name,
of a variable or of something else. In Pike an identifier must start
with an alphabetical letter or an underscore character ("_"). The rest
of the characters can be alphabetical letters, underscores, and
digits. Some valid identifiers in Pike are <tt>n</tt>,
<tt>number_of_9s</tt>, and <tt>DoNotFeedTheMonkey</tt>.</p>

<h1>Data type</h1>

<p>Values and variables have <b>data types</b>. The value <b>5</b> has
the data type <b>integer</b> (spelled <tt>int</tt> in Pike), and
<b>5.14</b> has the type real number (which is spelled <tt>float</tt>
in Pike). You can only put values in a variable if the types of the
value and the variable are compatible.</p>

<h1>Type checking</h1>

<p>Pike has type checking, which means that Pike keeps track of the
data types of variables and values. If your program tries to put one
type of value in a variable which was designed to hold another type of
value, Pike may detect this, and tell you about the problem.</p>

<h1>Expression</h1>

<p>An expression is a piece of a program that gives a value when it is
executed by the computer. An example of an expression in Pike is <tt>5
* x + 7</tt>, which means "take the value of the variable <tt>x</tt>
and multiply it with <b>5</b>, and then add <b>7</b>". An expression
also has a type, and in this case the type is <tt>int</tt>.</p>

<h1>Statement</h1>

<p>A statement is a command that is part of a program, and that the
computer can interpret and execute.</p>

<h1>Control structure</h1>

<p>The instructions in a program must be executed in the right order,
and this order is expressed using control structures. Examples of
control structures are selection (such as the <b>if</b> statement) and
loops (such as the <b>while</b> loop). More about those later.</p>

<h1>True and false</h1>

<p>Control structures such as <b>if</b> do one thing or another,
depending on a condition. If the condition is true, it does one thing,
and if it is false, it does another thing. Some languages have special
data values, called "true" and "false", that are used for this. In
Pike, we use the simpler convention that the value zero (<b>0</b>) is
interpreted as false, and everything else is interpreted as true.</p>

<h1>Truth value</h1>

<p>True and false are sometimes called <b>truth values</b>.</p>

<h1>Block</h1>

<p>A <b>block</b> or <b>compound statement</b> is a statement that
consists of several other statements. In Pike, we use the curly
brackets "{" and "}" to group statements into blocks. Some other
languages use the words "begin" and "end".</p>

<h1>Function</h1>

<p>Most programming languages allow you to divide your program into
smaller parts. These can be called "sub-routines", "procedures",
"functions" or "methods". A function receives some values as input,
and produces some values as output. A simple example is a function
called "plus", which receives two integers, called "x" and "y", and
returns their sum. In mathematical notation, this would be written
something like "plus(x, y) = x + y". In Pike, we write "<tt>int
plus(int x, int y) { return x + y; }</tt>".</p>

<h1>Method</h1>

<p>In object-oriented programming (which is explained more below),
functions belong to a certain class, and they work mainly with the
data belonging to that class. Such a function, which belongs to a
class, is usually called a "method". In Pike, all the functions that
you define are actually methods. We will use the term "method" in the
rest of this tutorial.</p>

<h1>Method call</h1>

<p>When one part of program needs to execute another part of the
program, and that other part is a method, we say that it "calls" that
method. in Pike, we would write "<tt>plus(3, 5)</tt>" to call the
method <tt>plus</tt> mentioned above. The values 3 and 5 are then sent
to the method, it adds them, and the result 8 is sent back from the
method.</p>

<h1>Argument</h1>

<p>When we call a method we send values to it. The values that are
sent to the method are called "arguments", or "actual arguments". In
the example with the call to <tt>plus</tt> above, the arguments are
<tt>3</tt> and <tt>5</tt>.</p>

<h1>Parameter</h1>

<p>A method needs to have some names that it can use to refer to the
argument values that are sent to it when it is called. Thes names are
called "parameters" or "formal parameters". In the example with the
method <tt>plus</tt> above, the parameters are <tt>x</tt> and
<tt>y</tt>. In most programming languages, and in Pike, the parameters
work just like variables which are local in the method, and which get
the arguments as initial values.</p>

<h1>Local</h1>

<p>A variable that is local in a piece of code, such as a method, is
only available to the code inside that piece. Other methods can have
their own local variables with the same name, but they are all
different variables: different boxes to store things in.</p>

<h1>Returning from a method</h1>

<p>When a method has finished what it has to do, we say that it
<b>returns</b>. The program will then continue executing immediately
after the place of the method call. If the method has produced a
value, we say that we <b>return that value</b>.</p>

<h1>Object-oriented programming</h1>

<p>In object-oriented programming, we do not only divide the program
into subprograms ("functions" or "methods"), but group data and
methods into "classes". This makes it easier to write programs, since
we can work with one piece (that is, class) at a time.</p>

<h1>Class</h1>

<p>This is a sort of "module", used in object-oriented programming.
Usually a class is a description of a type of thing (such as "cat",
"animal" or "Internet connection"), and it has both data and
operations: animals have a weight and a color, and the Internet
connection can change the connecting speed and close the connection.
In Pike, the word <b>program</b> is sometimes used to mean class.</p>

<h1>Object</h1>

<p>A class is a description of a type of thing (such as "cat"), and an
object is one such thing (such as one particular cat). Sometimes we
say that an object is an <b>instance</b> or a <b>clone</b> of a class:
all cats are instances of the cat class.</p>

<h1>Source code</h1>

<p>The source code is the program itself, as written by the
programmer. The source code is usually stored in one or more text
files.</p>

<h1>Syntax</h1>

<p>The syntax for a programming language is the grammar for the
language, i. e. the rules for how the source code is written. The
syntax only describes how the language looks, not what it actually
does.</p>

<h1>Compilation</h1>

<p>The process of translating the source code to a format that the
computer can execute. This format can be machine code, which is
directly executable by the computer. It can also be an intermediate
format, which has to be interpreted by another program.</p>

</manual>
