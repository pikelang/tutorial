<manual>

<p>This tutorial gives an introduction to the language Pike. It is not
a complete reference manual. It does not document the modules and
libraries that come with the Pike distribution, except for what is
necessary to explain the workings of the language itself. We also
assume that Pike has already been installed on your computer. How to
install Pike is explained elsewhere.</p>

<p>The tutorial is mainly intended for people with at least some
experience of programming, for example from writing some JavaScript on
a web page, or trying to write CGI scripts on a web server in some
language. We assume that you now have a need for, or just an interest
in, using Pike. We also assume that you are willing to spend a few
hours on reading this introduction. We recommend that you try to run
the examples as you read them, since this will help your learning
greatly.</p>

<p>Even total beginners at programming will be able to understand much
of what is said. If you have never programmed before it is
nevertheless a good idea to read a book or tutorial intended for
beginners. There are some general skills and ways of thinking that are
needed to write good programs, and a language tutorial such as this
one will not cover those in any depth.</p>

<p>We have tried to make this tutorial platform-independent, meaning
that it will not matter if you are using Pike under Linux, Solaris,
Windows NT, or some other operating system.</p>

<p>The following conventions for type faces are used:</p>

<list type="ul">
<item>
<p><b>Boldface</b> is used for terms that have a special meaning.
Example:</p>

<p>A <b>variable</b> can be seen as a sort of box where you can store
a value. It can also be used to show values, such as <b>7.3</b>.</p>
</item>

<item>
<p><tt>Fixed size</tt> is used for something that is copied verbatim
from the computer. It can be parts of a program, or something printed
by the computer, or something typed by the user. Example:</p>

<p>In Pike, the datatype integer is spelled <tt>int</tt>.</p>
</item>

<item>
<p><i>Italics</i> can be used for emphasis, but also as a placeholder
for other things. Example:</p>

<p>In Pike, you can define a variable with "<i>datatype</i>
<i>name</i><tt>;</tt>", where you replace <i>name</i> with the name of
the variable, and <i>datatype</i> with its type.</p>
</item>

<item>

<p>A bordered section of fixed-size text on a blue background
represents an interactive session, where user input is marked in
dark blue italics. The rest of the session's text is program
output:</p>

<interactive><out>Hello, professor.
I see you brought the keys to the ferarri.
&gt; </out><in>run project 21</in>
<out>Warming up particle accelerator...</out>
</interactive>

</item>

<item>
<p>Widthin fixed-width blocks of pike code in pike examples,
changes are marked in dark red bold text, as in:</p>

<pi>void example()
{
<changed>  write("This line was changed from the previous example.\n");</changed>
}
</pi>
</item>

</list>

</manual>
