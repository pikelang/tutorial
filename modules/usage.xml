<metadata>
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="How Do You Use a Module?">
</metadata>
<manual>

<p>To use a member of a module, just prefix the member name with the
module name and a dot. For example, we have already used the standard
input, which is called <tt>Stdio.stdin</tt>. That means that it is the
member <tt>stdin</tt> in the module <tt>Stdio</tt>. In this example,
we read a string from the standard input by calling the method
<tt>gets</tt> in the object <tt>Stdio.stdin</tt>:</p>

<pi>string name = Stdio.stdin-&gt;gets();</pi>

<p>We have also used the class <tt>Protocols.HTTP.Query</tt>, which is
the member <tt>Query</tt> in the module <tt>Protocols.HTTP</tt>. Note
that the module <tt>Protocols.HTTP</tt> is actually the module
<tt>HTTP</tt>, which is a member of the module <tt>Protocols</tt>. In
this example, we use the class <tt>Protocols.HTTP.Query</tt> as the
data type when defining a variable:</p>

<pi>Protocols.HTTP.Query web_page;</pi>

<p>If we don't want to write the module name every time we use
something from that module, we can <b>import</b> the module.</p>

<pi>
import Stdio;
import Protocols.HTTP;
</pi>

<p>After importing those two modules, we can use both <tt>stdin</tt>
and <tt>Query</tt> without prefixing them with anything:</p>

<pi>
string name = stdin-&gt;gets();
Query web_page;
</pi>

<p>Importing can be done anywhere in a program,
but we usually do it at the top of the program.</p>

<p>If we import two modules that contain the same name, that name will
refer to the module that was imported latest. An example where the
modules <tt>canvas_window</tt> and <tt>chess_game</tt> both contain
the method <tt>draw</tt>:</p>

<pi>
import canvas_window;
draw(); // Lets you draw pictures
import chess_game;
draw(); // A draw in a chess game: nobody wins
</pi>

<p>Even if you have imported a module, you can use the module name to
determine which module you mean:</p>

<pi>canvas_window.draw(); // Lets you draw pictures</pi>

<p>You can import all the modules in a directory by giving the
directory name as a string:</p>

<pi>import "/home/blubbers/pike/all_my_modules";</pi>

<p>If you refer to a module with the module name prefixed by just a
dot, Pike will look for the module in the same directory as the
program that used the module:</p>

<pi>
import .florble;
.gnooble.droop(7, 3);
</pi>

</manual>
