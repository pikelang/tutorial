<manual>

<p>Here is a somewhat detailed explanation of exactly how Pike finds
the modules that you tell it to use. Normally you don't really need to
worry about all the details, except for what is needed to put your own
modules in a place where Pike can find them. But sometimes the details
can be important, for example if there are several modules with the
same name, but in different places, or if you want to do something
advanced and ingenious with modules.</p>

<p>Every module that is used somewhere in your program is loaded when
the program starts. It doesn't matter if you are importing the entire
module, or if you are just using a "qualified" name such as
<tt>smorglemod.fnordle()</tt> (that is, the method <tt>fnordle</tt> in
the module <tt>smorglemod</tt>). This is done even before
<tt>main</tt> is called. That the module is loaded means that the file
is read from disk, and the things in the module are made accessible to
your program.</p>

<p>When Pike loads a module with the name <tt><i>module-name</i></tt>,
Pike will look for a module with that name in the following
directories, in this order:</p>

<list type="ol">
<item>
<p>If the <tt><i>module-name</i></tt> was prefixed with a dot, in the
same directory as the program that used the module</p>
</item>

<item>
<p>In directories that have been imported with <tt>import
"<i>directory-name</i>"</tt></p>
</item>

<item>
<p>In directories added with <tt>add_module_path</tt></p>
</item>

<item>
<p>In directories specified with <tt>-M</tt> on the command line</p>
</item>

<item>
<p>In directories listed in the environment variable
<tt>PIKE_MODULE_PATH</tt></p>
</item>

<item>
<p>In the directory with builtin modules, for example called
<tt>/usr/local/pike/7.4.1/lib/modules/</tt> or
<tt>C:\Program&nbsp;Files\Pike\lib\pike\modules</tt></p>
</item>
</list>

<p>In each of those directories, Pike will look for files or
subdirectories, in this order:</p>

<list type="ol">
<item>
<p>If there is a file called <tt><i>module-name</i>.pmod</tt>, Pike
will load that file as a Pike program, clone it, and use the
newly-cloned object as a module.</p>
</item>

<item>
<p>If there is a directory called <tt><i>module-name</i>.pmod</tt>,
Pike will create a module that has all the modules in that directory
as members. If there is a module called <tt>module</tt> (that is, with
the file name <tt>module.pmod</tt>, <tt>module.pmod.pike</tt>, or
<tt>module.pmod.so</tt>) in the directory
<tt><i>module-name</i>.pmod</tt>, the members from that module will be
used instead of the modules actually present in the directory.</p>
</item>

<item>
<p>If there is a file called <tt><i>module-name</i>.pmod.pike</tt>,
Pike will load that file as a Pike program, clone it, and use the
newly-cloned object as a module.</p>
</item>

<item>
<p>If there is a file called
<tt><i>module-name</i>.so</tt> Pike will load that file with
<tt>load_module</tt>, clone it, and use the newly-cloned object as a
module. The file should be a dynamically linkable library of compiled
C or C++ code.</p>
</item>
</list>

<p>As we said before, you usually don't need to worry about these
details, except for what is needed to put your modules somewhere where
Pike can find them.</p>

</manual>
