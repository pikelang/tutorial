<metadata>
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="I Want My Greeting in a Window!">
</metadata>
<manual>

<p>Pike has support for graphical user interfaces. If you have the GTK
Pike module installed on your computer, you can use a slightly
modified program to print "Hello world!" in its own window:</p>

<pi>
int main()
{
  GTK.setup_gtk();
  GTK.Alert("Hello world!");
  return -1;
}
</pi>

<p>The statement "<tt>GTK.setup_gtk();</tt>" is a call to a method,
similar to the call to <tt>write</tt> in our first example. The
difference here is that <tt>setup_gtk</tt> is found in a module called
<tt>GTK</tt>, so we must prefix it with "<tt>GTK.</tt>" to let Pike
know where to look for it.</p>

<p>The next statement, "<tt>GTK.Alert("Hello world!");</tt>", creates
a small window, an "alert window", with the text "Hello world!" in it.
The window will look something like this:</p>

<image src="alertwindow.gif" caption="The window that pops up when the GTK hello-world program is run"/>

<p>When you click on the "OK" button, the window disappears.</p>

<p>The last statement in <tt>main</tt> is "<tt>return -1;</tt>". In
Pike, a negative return value from <tt>main</tt> means that the
program doesn't stop executing when <tt>main</tt> is finished. This is
necessary here, since otherwise the program would finish as soon as it
had created the window, and the window would disappear at once.</p>

<p>But there is a problem with this program. The program doesn't stop
executing when <tt>main</tt> is finished, so when does it stop? Never.
We may close the window, but the program is still running. It doesn't
actually do anything, but it is there.</p>

<p>We can fix the problem like this:</p>

<pi>
int main()
{
  GTK.setup_gtk();
  GTK.Alert("Hello world!") -&gt; signal_connect("destroy", exit, 0);
  return -1;
}
</pi>

<p>To explain this, think that</p>

<pi>GTK.Alert("Hello world!")</pi>

<p>creates the Alert window. The Alert window is a thing, or an
"object". It not only pops up on your computer screen, but you can
also let your program do things with this object. For example, you can
tell the window that when it is destroyed, it should order the entire
program to exit. This is what the rest of of the statement does (even
if we don't explain the exact details here):</p>

<pi>-&gt; signal_connect("destroy", exit, 0);</pi>

</manual>
