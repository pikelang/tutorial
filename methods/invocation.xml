<metadata>
 <datum name=__commit_id valuen=19103>
 <datum name=__created valuen=0>
 <datum name=__ext_created valuen=1411269007>
 <datum name=__uuid value="6eb620ad-b8da-4198-90b7-8c281d7bcb70">
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Calling a method">
</metadata>
<manual>

<p>First of all, Pike calculates the values of all the arguments. In
the call</p>

<pi>average(19.0 + 11.0, 10.0)</pi>

<p>the two arguments are calculated, giving the values <b>30.0</b> and
<b>10.0</b>. Then the argument values are sent to the method. If we
look at the method head,</p>

<pi>float average(float x1, float x2)</pi>

<p>we see that it has two formal parameters. The argument values will
be put in the two parameter variables <tt>x1</tt> and <tt>x2</tt>,
which work as local variables but with the argument values as initial
values.</p>

<p>Execution will then continue with the body of the method. In this
case, the body is</p>

<pi>
{
  return (x1 + x2) / 2;
}
</pi>

<p>The value of <tt>(x1 + x2) / 2</tt> will be calculated, giving
<b>20.0</b>. This value is then returned to the point where the method
was called, and is used as the value of the method-call
expression.</p>

<p>Note that Pike uses "call by value". This means that Pike always
calculates the value of the arguments before calling a method, and
then it sends those values (or, more precisely: copies of those
values) to the called method. This means that in the example</p>

<comment>Wrong!</comment>

<pi>average(1.0, average(2.0, 3.0))</pi>

<p>Pike will first call <tt>average</tt> with the two values
<b>2.0</b> and <b>3.0</b>, and when <tt>average</tt> has returned the
value <b>2.5</b>, it will send the two values <b>1.0</b> and
<b>2.5</b> to <tt>average</tt>. This second call of <tt>average</tt>
will return <b>1.75</b>, and this is the value of the entire
expression.</p>

</manual>
