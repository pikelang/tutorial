<metadata>
 <datum name=__removed valuen=1>
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Methods: an Introduction">
</metadata>
<manual>

<p>Most programming languages allow you to divide your program into
smaller parts. These can be called "sub-routines", "procedures",
"functions" or "methods". In Pike, we use the term "method". Other
parts of the program can <b>call</b> the method, i. e. cause it to be
executed.</p>

<h1>How to create a method</h1>

<p>To create a method, you must <b>define</b> it, with a <b>method
definition</b>. A method definition follows this template:</p>

<pi>
<i>access-modifiers</i> <i>type</i> <i>method-name</i>( <i>parameter-list</i> )
<i>method-body</i>
</pi>

<p>Here is a description of the various parts in the definition
template:</p>

<list type="ul">
<item>
<p>The <i>access-modifiers</i> are optional. If they are present, they
control from where this method can be called. An example of an
access-modifier is <tt>private</tt>, which makes it impossible for
other programs to call this method.</p>
</item>

<item>
<p>The <i>type</i> is a <b>data type</b>, which specifies the type of
the value that the method returns. It is sometimes called the "return
type of the method", or the "type of the method". An example of a data
type is <tt>string</tt>. If the type is <tt>void</tt>, the method is
not supposed to return a value.</p>
</item>

<item>
<p>The <i>method-name</i> is the name of the method. This is an
identifier, for example <tt>plus</tt>,
<tt>destroy_all_enemy_ships</tt> or <tt>mUndoLatestChange</tt>. A
method should have a name that correctly describes what the method
does: a method that prints a list of customers should probably be
called something like <tt>print_customers</tt>.</p>
</item>

<item>
<p>The <i>parameter-list</i> is a comma-separated list of the
parameters of the method. A <b>parameter</b> acts like a variable,
which is local in the method, and which gets the corresponding
argument value from the method call as its initial value. Some
examples of parameters are "<tt>int number_of_cars</tt>" and
"<tt>string name</tt>". A method can have up to 256 parameters. If the
method doesn't expect to receive any argument values, the
<i>parameter-list</i> can be empty.</p>
</item>

<item>
<p>The <i>method-body</i> is a <b>block</b>, and can contain
statements and local definitions.</p>
</item>
</list>

<p>Sometimes we talk of the <b>head</b> and the <b>body</b> of a
method. The body is of course the <i>method-body</i> in the template
above, while the head consists of everything in the method definition
except the body. We can say that the method head is a description of
the method: its name, which arguments it expects, and what type of
value it will return. A part of the program that wants to <b>call</b>
a method, needs to know about the head of that method, but not about
the body.</p>

<p>The <b>method body</b>, on the other hand, contains the statements
that will be executed when the method is called. The body is therefore
a description of what, and how, the method performs whatever it is
that it does.</p>

<p>Here is a simple example of a method definition:</p>

<pi>
float average(float x1, float x2)
{
  return (x1 + x2) / 2;
}
</pi>

<p>The method <tt>average</tt> returns the average of its two
arguments. Both the return value and the two parameters are
floating-point values. Here are some valid statements that contain
calls to <tt>average</tt>:</p>

<pi>
float x = average(19.0 + 11.0, 10.0);
average(27.13, x + 27.15);
float y = average(1.0, 2.0) + average(6.0, 7.1);
float z = average(1.0, average(2.0, 3.0));
</pi>

<p>When a method has finished what it has to do, we say that it
<b>returns</b>. The program will then continue executing immediately
after the place of the method call. If the method has produced a
value, we say that we return that value.</p>

<p>The <tt>return</tt> statement is used to send a value from a method
back to the point from where it was called:</p>

<pi>return <i>expression</i>;</pi>

<p>The <tt>return</tt> statement will also cause Pike to leave the
method, and continue execution immediately after the point where the
call to the method was made. You can have several <tt>return</tt>
statements in the same method. If the method is defined to return
<tt>void</tt>, you can use <tt>return</tt> without a value to leave
the method:</p>

<pi>return;</pi>

<p>If you reach the end of the body of a method, without having
returned first, the method will return zero as return value.</p>

</manual>
