<metadata>
 <datum name=__commit_id valuen=15>
 <datum name=__create_rev value="1.3">
 <datum name=__created valuen=1039803517>
 <datum name=__ext_created valuen=1039803517>
 <datum name=__uuid value="d9090e66-b3b1-4a6d-8dd4-0af70c99a2f7">
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Your Second Pike Program">
</metadata>
<manual>

<p>Your first Pike program, which was examined above in this tutorial,
was a program that printed "Hello world!" on the screen. Your second
Pike program will be a World Wide Web browser, just like Netscape or
Internet Explorer.</p>

<p>Well, perhaps not <i>just</i> like Netscape or Internet Explorer.
Both of those are very large programs, and our browser will be a very
small program. We will just make it advanced enough to connect to a
World Wide Web server, download a web page, and then display it on the
screen.</p>

<p>We start by creating a file, for example called
<tt>webbrowser.pike</tt>:</p>

<pi>
// The Very Simple World Wide Web Browser

int main()
{
  write("Welcome to the Very Simple WWW Browser!\n");
  return 0;
} // main
</pi>

<p>This is almost the same program as the "hello world" program. The
only difference is that it prints something else, and that we have
added some <b>comments</b>. Two "slash" characters (<tt>//</tt>) means
that the rest of the line is a comment. Comments are ignored by Pike,
and are intended for humans who read your program.</p>

<p>Pike has another type of comments, which start with <tt>/*</tt>
and end with <tt>*/</tt>. Such comments can span several lines:</p>

<pi>
/*
This is also a comment.
For example, you can "comment out" parts
of your program with this type of comment,
so Pike doesn't run those parts.
*/
</pi>

<h1>Variables</h1>

<p>The web browser must be told which web page it should download and
display. As you may know, the "addresses" to web pages are called URLs
(Uniform Resource Locators), and they look like
<tt>pike.idonex.se</tt> or <tt>http://pike.idonex.se/</tt> (where the
second form is correct, but the first one usually works too).</p>

<p>We add some code that lets the user type a URL, stores that URL in
a variable, and also prints it on the screen as confirmation. The new
additions are in bold face:</p>

<pi>
// The Very Simple World Wide Web Browser

int main()
{
  write("Welcome to the Very Simple WWW Browser!\n");
  <changed>string url;
  write("Type the address of the web page:\n");
  url = Stdio.stdin-&gt;gets();
  write("URL: " + url + "\n");</changed>
  return 0;
} // main
</pi>

<p>There are four new things here:</p>

<list type="ul">
<item>
<p>We create a <b>variable</b> with the name <tt>url</tt>. This is
called to <b>define</b> a variable. The variable has the type
<tt>string</tt>, which means that we an use it to store strings in. A
string is a sequence of characters.</p>
</item>

<item>
<p><pi>Stdio.stdin-&gt;gets()</pi> is used to let the user type some
text on the keyboard. When the user hits the return key, the text is
returned as a string. (<pi>Stdio.stdin-&gt;gets()</pi> calls the
method <tt>gets</tt> in the object <tt>stdin</tt> from the module
<tt>Stdio</tt>, but you don't need to worry about that just yet.)</p>
</item>

<item>
<p>We take that string, and store it in the variable. This is called
<b>assignment</b>, or to <b>assign</b> a value to the variable.</p>
</item>

<item>
<p>In the last line with <tt>write</tt>, we add three strings
together:</p>

<pi>"URL: " + url + "\n"</pi>
</item>
</list>

<p>When we run the program, it may look something like this.
The user's commands and input are shown in <i>italics</i>:</p>

<interactive>
<in>pike webbrowser.pike</in>
<out>
Welcome to the Very Simple WWW Browser!
Type the address of the web page:
</out>
<in>http://pike.ida.liu.se/</in>
<out>URL: http://pike.ida.liu.se/</out>
</interactive>

</manual>
