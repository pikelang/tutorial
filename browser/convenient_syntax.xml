<metadata>
 <datum name=__commit_id valuen=19180>
 <datum name=__created valuen=0>
 <datum name=__ext_created valuen=1411269033>
 <datum name=__uuid value="976ed6d9-dd37-461b-84dd-817c2a80a331">
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Syntactic Sugar">
</metadata>
<manual>

<h1>Importing a Module</h1>

<p>If we don't want to write the module name every time we use
something from that module, an alternative is to <b>import</b> the
module. If we import <tt>Protocols.HTTP</tt>, we can use the data type
<tt>Query</tt>, and the method <tt>get_url</tt>, without prefixing
them with <tt>Protocols.HTTP</tt>:</p>

<pi>
<changed>import Protocols.HTTP;</changed>

void handle_url(string this_url)
{
  write("Fetching URL '" + this_url + "'...");
  <changed>Query</changed> web_page;
  web_page = <changed>get_url</changed>(this_url);
  if(web_page == 0)
  {
    write(" Failed.\n");
    return;
  }
  write(" Done.\n");
} // handle_url
</pi>

<p>Although you <i>could</i> import lots and lots of modules for the
ease of lazy typing, this mostly isn't a recommended practice, for
obvious reasons of clarity and readability. There are also some non-obvious
reasons to refrain from doing imports. If someone adds the method
<tt>write</tt> to the module <tt>Protocols.HTTP</tt> we would call that
method instead of the one that writes text to the user. It also takes longer
to start the program, since Pike must search through all imported modules
to find the methods you use.</p>

<h1>Initial Values for Variables</h1>

<p>We can give a value to a variable when we define it, so there is no
reason to write:</p>

<pi>
  Query web_page;
  web_page = get_url(this_url);
</pi>

<p>We change it to this:</p>

<pi>
  <changed>Query web_page = get_url(this_url);</changed>
</pi>

</manual>
