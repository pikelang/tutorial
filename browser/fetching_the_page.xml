<metadata>
 <datum name=__commit_id valuen=19178>
 <datum name=__created valuen=0>
 <datum name=__ext_created valuen=1411269032>
 <datum name=__uuid value="dbff9133-f8f7-4914-abbb-6c1fbe3f618b">
 <datum name=author value="0">
 <datum name=description value="">
 <datum name=external_use value="">
 <datum name=http-content-type value="text/xml">
 <datum name=keywords value="">
 <datum name=stationery value="">
 <datum name=stationery_md_flags value="">
 <datum name=template value="tutorial.xsl">
 <datum name=title value="Magic Web Stuff">
</metadata>
<manual>

<p>Now it's time to start surfing the web! Perhaps you know that web
pages are written in something called HTML (HyperText Markup
Language), and that the <tt>http</tt> you see in web addresses like
<tt>http://pike.ida.liu.se/</tt> means HyperText Transfer Protocol. The
HyperText Transfer Protocol is a description of how web browsers
communicate with web servers. Netscape and Internet Explorer use it,
and so will we.</p>

<p>It is actually a fairly complicated operation to connect to a web
server, to tell it to send you a web page, and then to receive that
page as the server sends it. Fortunately for us, someone has already
done this for us. There is a <b>module</b> called
<tt>Protocols.HTTP</tt>, which handles the communication with the web
server. A module is a package of Pike code that can easily be used by
other programmers.</p>

<p>We rewrite the method <tt>handle_url</tt> to actually try to fetch
the web page, using the module <tt>Protocols.HTTP</tt>:</p>

<pi>
void handle_url(string this_url)
{
  <changed>write("Fetching URL '" + this_url + "'...");
  Protocols.HTTP.Query web_page;
  web_page = Protocols.HTTP.get_url(this_url);
  if(web_page == 0)
  {
    write(" Failed!\n");
    return;
  }
  write(" Done.\n");</changed>
} // handle_url
</pi>

<p>The interesting part here is the lines</p>

<pi>
  Protocols.HTTP.Query web_page;
  web_page = Protocols.HTTP.get_url(this_url);
</pi>

<p>First we define the variable <tt>web_page</tt>, with the data type
<tt>Protocols.HTTP.Query</tt>. Actually, the data type is called
<tt>Query</tt>, and is defined in the module <tt>Protocols.HTTP</tt>,
but we must write it as <tt>Protocols.HTTP.Query</tt> so Pike knows
where to find it.</p>

<p>A data item of the type <tt>Protocols.HTTP.Query</tt> contains the
result of a web page retrieval: the text of the web page, but also
some more information, such as the time when the page was created.</p>

<p>The actual work is done by the method
<tt>Protocols.HTTP.get_url</tt>, which is actually the method
<tt>get_url</tt> in the module <tt>Protocols.HTTP</tt>. It talks to
the web server, fills a <tt>Query</tt> object with everything it
finds, and returns it. If it cannot find the web page, it returns zero
(<b>0</b>) instead.</p>

<p>Some other things that might need to be explained in this
example:</p>

<list type="ul">
<item>
<p>We can use single quotes (') inside a string. If we want to put a
double quote (&quot;) in a string, we can do so by prefixing the
double quote with a backslash:</p>

<p>
<pi>"This string contains a \&quot;."</pi>
</p>
</item>

<item>
<p>If the web page couldn't be found, we use the statement</p>

<pi>return;</pi>

<p>to stop executing the method <pi>handle_url</pi>, and instead
return to where it was called. This is the same as the</p>

<pi>return 0;</pi>

<p>we have seen in <pi>main</pi>, except that <pi>handle_url</pi>
doesn't return a value.</p>
</item>

<item>
<p><pi>return</pi> just returns from the method we are in. If we want
to terminate the program, we can use the built-in function
<pi>exit</pi>:</p>

<pi>exit(0);</pi>

<p>This has the same effect as returning <b>0</b> from
<pi>main</pi>.</p>
</item>
</list>

<p>When we run this version of the web browser, it may look something
like this. The user's command is shown in <i>italics</i>:</p>

<interactive>
<in>webbrowser.pike pike.ida.liu.se</in>
<out>
Welcome to the Very Simple WWW Browser!
Fetching URL 'pike.ida.liu.se'... Done.
</out>
</interactive>

<p>If we try to retrieve a web page that doesn't exist, the web
browser fails:</p>

<interactive>
<in>webbrowser.pike cod.ida.liu.se</in>
<out>
Welcome to the Very Simple WWW Browser!
Fetching URL 'cod.ida.liu.se'... Failed!
</out>
</interactive>

</manual>
