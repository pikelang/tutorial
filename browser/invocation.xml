<manual>

<p>An appropriate next step would be making your program run without
the need for manually invoking it with the pike parser. If you are
using a Unix system, such as Linux or Solaris, you can make the web
browser a free-standing program by adding:</p>

<pi>
#! /usr/local/bin/pike
</pi>

<p>(or "<tt>#! </tt>" followed by whatever Pike itself happens to be
called on your system) as the very first line in the file, without any
spaces in the part pointing out the path to your pike binary.</p>

<p>Assuming you want your script to run with whatever "pike" binary
would be run if "pike" was entered at the prompt, that is, the first
"pike" executable found in the user's path, a useful and portable
alternative is this syntax:</p>

<pi>
#! /usr/bin/env pike
</pi>

<p>Either way, you must finish off the work by making the file
executable:</p>

<interactive><in>chmod a+x webbrowser.pike</in></interactive>

<p>Now, you can run the web browser just by giving the command:</p>

<interactive><in>webbrowser.pike</in></interactive>

<p>or by clicking on its icon in a graphic file manager. (If you don't
like the extension <tt>.pike</tt>, you can simply change the name of
the file to <tt>webbrowser</tt>, without the extension.)</p>

<p>Under Windows NT, you can associate the file extension
<tt>pike</tt> with the Pike program. Then you can start the web
browser by clicking on the web browser's icon, or by giving the
command <tt>webbrowser.pike</tt> in the Command Prompt Window.</p>

</manual>
